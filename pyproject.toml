[project]
name = "agent-polaris"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.121.0",
    "pydantic-settings>=2.11.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
